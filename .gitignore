node_modules
dist
dist-ssr
.vite

# npm auth — NUNCA commitar, contém token
.npmrc

# Vite env: commit `.env.development|staging|production`,
# ignore literal `.env` and any `*.local` overrides
.env
.env.local
.env.*.local
*.local

# Build / IDE caches
*.tsbuildinfo
storybook-static
.claude/

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
